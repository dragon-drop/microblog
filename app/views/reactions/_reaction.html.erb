<% if user_signed_in? %>
  <%= form_tag reactions_path, method: :post do %>
    <%= hidden_field_tag :post_id, post.id %>
    <%= render 'reactions/button', post: post, button_type: 'submit' %>
  <% end %>
<% else %>
  <%= render 'reactions/button', post: post, button_type: 'button' %>
<% end %>
