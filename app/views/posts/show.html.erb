<%= content_for :title do %>
  <%= render "shared/back_link" %>
  <%= @post.user.name %> said:
<% end %>

<div class="text-3xl tracking-tight">
  <h2 class="font-bold"><%= @post.user.name %> said:</h2>
  <p><span class="font-black mr-1">&gt;</span> <%= line_breaks @post.body %></p>
  <p class="font-light flex flex-col">
  <%= timeago(@post.created_at) %>
  <span>on <%= @post.created_at.to_s(:long) %></span></p>
  <div>Total count of likes: <span id="users_likes_total_count_<%= @post.id %>"><%= @post.users_likes_total_count %></span></div>
  <% if current_user %>
    <div id="like-post-<%= @post.id %>" class="flex">
      <%= render (@post.liked_by?(current_user) ? 'liked': 'like'), post: @post %>
    </div>
  <% end %>
</div>
